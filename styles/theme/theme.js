import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#333333',
    },
    secondary: {
      main: '#50C878',
    },
    success: {
      main: "#00ED64",
    }
  },
});

export default theme;